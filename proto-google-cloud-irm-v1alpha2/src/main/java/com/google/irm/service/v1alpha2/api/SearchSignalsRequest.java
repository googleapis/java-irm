/*
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// Generated by the protocol buffer compiler.  DO NOT EDIT!
// source: google/cloud/irm/v1alpha2/incidents_service.proto

package com.google.irm.service.v1alpha2.api;

/**
 *
 *
 * <pre>
 * Request for the SearchSignals method.
 * </pre>
 *
 * Protobuf type {@code google.cloud.irm.v1alpha2.SearchSignalsRequest}
 */
public final class SearchSignalsRequest extends com.google.protobuf.GeneratedMessageV3
    implements
    // @@protoc_insertion_point(message_implements:google.cloud.irm.v1alpha2.SearchSignalsRequest)
    SearchSignalsRequestOrBuilder {
  private static final long serialVersionUID = 0L;
  // Use SearchSignalsRequest.newBuilder() to construct.
  private SearchSignalsRequest(com.google.protobuf.GeneratedMessageV3.Builder<?> builder) {
    super(builder);
  }

  private SearchSignalsRequest() {
    parent_ = "";
    query_ = "";
    pageToken_ = "";
  }

  @java.lang.Override
  @SuppressWarnings({"unused"})
  protected java.lang.Object newInstance(UnusedPrivateParameter unused) {
    return new SearchSignalsRequest();
  }

  @java.lang.Override
  public final com.google.protobuf.UnknownFieldSet getUnknownFields() {
    return this.unknownFields;
  }

  private SearchSignalsRequest(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    this();
    if (extensionRegistry == null) {
      throw new java.lang.NullPointerException();
    }
    com.google.protobuf.UnknownFieldSet.Builder unknownFields =
        com.google.protobuf.UnknownFieldSet.newBuilder();
    try {
      boolean done = false;
      while (!done) {
        int tag = input.readTag();
        switch (tag) {
          case 0:
            done = true;
            break;
          case 10:
            {
              java.lang.String s = input.readStringRequireUtf8();

              parent_ = s;
              break;
            }
          case 18:
            {
              java.lang.String s = input.readStringRequireUtf8();

              query_ = s;
              break;
            }
          case 24:
            {
              pageSize_ = input.readInt32();
              break;
            }
          case 34:
            {
              java.lang.String s = input.readStringRequireUtf8();

              pageToken_ = s;
              break;
            }
          default:
            {
              if (!parseUnknownField(input, unknownFields, extensionRegistry, tag)) {
                done = true;
              }
              break;
            }
        }
      }
    } catch (com.google.protobuf.InvalidProtocolBufferException e) {
      throw e.setUnfinishedMessage(this);
    } catch (java.io.IOException e) {
      throw new com.google.protobuf.InvalidProtocolBufferException(e).setUnfinishedMessage(this);
    } finally {
      this.unknownFields = unknownFields.build();
      makeExtensionsImmutable();
    }
  }

  public static final com.google.protobuf.Descriptors.Descriptor getDescriptor() {
    return com.google.irm.service.v1alpha2.api.IncidentsService
        .internal_static_google_cloud_irm_v1alpha2_SearchSignalsRequest_descriptor;
  }

  @java.lang.Override
  protected com.google.protobuf.GeneratedMessageV3.FieldAccessorTable
      internalGetFieldAccessorTable() {
    return com.google.irm.service.v1alpha2.api.IncidentsService
        .internal_static_google_cloud_irm_v1alpha2_SearchSignalsRequest_fieldAccessorTable
        .ensureFieldAccessorsInitialized(
            com.google.irm.service.v1alpha2.api.SearchSignalsRequest.class,
            com.google.irm.service.v1alpha2.api.SearchSignalsRequest.Builder.class);
  }

  public static final int PARENT_FIELD_NUMBER = 1;
  private volatile java.lang.Object parent_;
  /**
   *
   *
   * <pre>
   * The resource name of the hosting Stackdriver project which requested
   * incidents belong to.
   * </pre>
   *
   * <code>string parent = 1;</code>
   *
   * @return The parent.
   */
  public java.lang.String getParent() {
    java.lang.Object ref = parent_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      parent_ = s;
      return s;
    }
  }
  /**
   *
   *
   * <pre>
   * The resource name of the hosting Stackdriver project which requested
   * incidents belong to.
   * </pre>
   *
   * <code>string parent = 1;</code>
   *
   * @return The bytes for parent.
   */
  public com.google.protobuf.ByteString getParentBytes() {
    java.lang.Object ref = parent_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b =
          com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
      parent_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int QUERY_FIELD_NUMBER = 2;
  private volatile java.lang.Object query_;
  /**
   *
   *
   * <pre>
   * An expression that defines which signals to return.
   * Search atoms can be used to match certain specific fields.  Otherwise,
   * plain text will match text fields in the signal.
   * Search atoms:
   * * `start` - (timestamp) The time the signal was created.
   * * `title` - The title of the signal.
   * * `signal_state` - `open` or `closed`. State of the signal.
   *   (e.g., `signal_state:open`)
   * Timestamp formats:
   * * yyyy-MM-dd - an absolute date, treated as a calendar-day-wide window.
   *   In other words, the "&lt;" operator will match dates before that date, the
   *   "&gt;" operator will match dates after that date, and the ":" operator will
   *   match the entire day.
   * * yyyy-MM-ddTHH:mm - Same as above, but with minute resolution.
   * * yyyy-MM-ddTHH:mm:ss - Same as above, but with second resolution.
   * * Nd (e.g. 7d) - a relative number of days ago, treated as a moment in time
   *   (as opposed to a day-wide span) a multiple of 24 hours ago (as opposed to
   *   calendar days).  In the case of daylight savings time, it will apply the
   *   current timezone to both ends of the range.  Note that exact matching
   *   (e.g. `start:7d`) is unlikely to be useful because that would only match
   *   signals created precisely at a particular instant in time.
   * The absolute timestamp formats (everything starting with a year) can
   * optionally be followed with a UTC offset in +/-hh:mm format.  Also, the 'T'
   * separating dates and times can optionally be replaced with a space. Note
   * that any timestamp containing a space or colon will need to be quoted.
   * Examples:
   * * `foo` - matches signals containing the word "foo"
   * * `"foo bar"` - matches signals containing the phrase "foo bar"
   * * `foo bar` or `foo AND bar` - matches signals containing the words
   *   "foo" and "bar"
   * * `foo -bar` or `foo AND NOT bar` - matches signals containing the
   *   word
   *   "foo" but not the word "bar"
   * * `foo OR bar` - matches signals containing the word "foo" or the
   *   word "bar"
   * * `start&gt;2018-11-28` - matches signals which started after November
   *   11, 2018.
   * * `start&lt;=2018-11-28` - matches signals which started on or before
   *   November 11, 2018.
   * * `start:2018-11-28` - matches signals which started on November 11,
   *   2018.
   * * `start&gt;"2018-11-28 01:02:03+04:00"` - matches signals which started
   *   after November 11, 2018 at 1:02:03 AM according to the UTC+04 time
   *   zone.
   * * `start&gt;7d` - matches signals which started after the point in time
   *   7*24 hours ago
   * * `start&gt;180d` - similar to 7d, but likely to cross the daylight savings
   *   time boundary, so the end time will be 1 hour different from "now."
   * * `foo AND start&gt;90d AND stage&lt;resolved` - unresolved signals from
   *   the past 90 days containing the word "foo"
   * </pre>
   *
   * <code>string query = 2;</code>
   *
   * @return The query.
   */
  public java.lang.String getQuery() {
    java.lang.Object ref = query_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      query_ = s;
      return s;
    }
  }
  /**
   *
   *
   * <pre>
   * An expression that defines which signals to return.
   * Search atoms can be used to match certain specific fields.  Otherwise,
   * plain text will match text fields in the signal.
   * Search atoms:
   * * `start` - (timestamp) The time the signal was created.
   * * `title` - The title of the signal.
   * * `signal_state` - `open` or `closed`. State of the signal.
   *   (e.g., `signal_state:open`)
   * Timestamp formats:
   * * yyyy-MM-dd - an absolute date, treated as a calendar-day-wide window.
   *   In other words, the "&lt;" operator will match dates before that date, the
   *   "&gt;" operator will match dates after that date, and the ":" operator will
   *   match the entire day.
   * * yyyy-MM-ddTHH:mm - Same as above, but with minute resolution.
   * * yyyy-MM-ddTHH:mm:ss - Same as above, but with second resolution.
   * * Nd (e.g. 7d) - a relative number of days ago, treated as a moment in time
   *   (as opposed to a day-wide span) a multiple of 24 hours ago (as opposed to
   *   calendar days).  In the case of daylight savings time, it will apply the
   *   current timezone to both ends of the range.  Note that exact matching
   *   (e.g. `start:7d`) is unlikely to be useful because that would only match
   *   signals created precisely at a particular instant in time.
   * The absolute timestamp formats (everything starting with a year) can
   * optionally be followed with a UTC offset in +/-hh:mm format.  Also, the 'T'
   * separating dates and times can optionally be replaced with a space. Note
   * that any timestamp containing a space or colon will need to be quoted.
   * Examples:
   * * `foo` - matches signals containing the word "foo"
   * * `"foo bar"` - matches signals containing the phrase "foo bar"
   * * `foo bar` or `foo AND bar` - matches signals containing the words
   *   "foo" and "bar"
   * * `foo -bar` or `foo AND NOT bar` - matches signals containing the
   *   word
   *   "foo" but not the word "bar"
   * * `foo OR bar` - matches signals containing the word "foo" or the
   *   word "bar"
   * * `start&gt;2018-11-28` - matches signals which started after November
   *   11, 2018.
   * * `start&lt;=2018-11-28` - matches signals which started on or before
   *   November 11, 2018.
   * * `start:2018-11-28` - matches signals which started on November 11,
   *   2018.
   * * `start&gt;"2018-11-28 01:02:03+04:00"` - matches signals which started
   *   after November 11, 2018 at 1:02:03 AM according to the UTC+04 time
   *   zone.
   * * `start&gt;7d` - matches signals which started after the point in time
   *   7*24 hours ago
   * * `start&gt;180d` - similar to 7d, but likely to cross the daylight savings
   *   time boundary, so the end time will be 1 hour different from "now."
   * * `foo AND start&gt;90d AND stage&lt;resolved` - unresolved signals from
   *   the past 90 days containing the word "foo"
   * </pre>
   *
   * <code>string query = 2;</code>
   *
   * @return The bytes for query.
   */
  public com.google.protobuf.ByteString getQueryBytes() {
    java.lang.Object ref = query_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b =
          com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
      query_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  public static final int PAGE_SIZE_FIELD_NUMBER = 3;
  private int pageSize_;
  /**
   *
   *
   * <pre>
   * Maximum number of `signals` to return in the response.
   * </pre>
   *
   * <code>int32 page_size = 3;</code>
   *
   * @return The pageSize.
   */
  public int getPageSize() {
    return pageSize_;
  }

  public static final int PAGE_TOKEN_FIELD_NUMBER = 4;
  private volatile java.lang.Object pageToken_;
  /**
   *
   *
   * <pre>
   * Page token from an earlier query, as returned in `next_page_token`. All
   * field values except for page_size and page_token should be the same as the
   * original query (may return an error or unexpected data otherwise).
   * </pre>
   *
   * <code>string page_token = 4;</code>
   *
   * @return The pageToken.
   */
  public java.lang.String getPageToken() {
    java.lang.Object ref = pageToken_;
    if (ref instanceof java.lang.String) {
      return (java.lang.String) ref;
    } else {
      com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
      java.lang.String s = bs.toStringUtf8();
      pageToken_ = s;
      return s;
    }
  }
  /**
   *
   *
   * <pre>
   * Page token from an earlier query, as returned in `next_page_token`. All
   * field values except for page_size and page_token should be the same as the
   * original query (may return an error or unexpected data otherwise).
   * </pre>
   *
   * <code>string page_token = 4;</code>
   *
   * @return The bytes for pageToken.
   */
  public com.google.protobuf.ByteString getPageTokenBytes() {
    java.lang.Object ref = pageToken_;
    if (ref instanceof java.lang.String) {
      com.google.protobuf.ByteString b =
          com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
      pageToken_ = b;
      return b;
    } else {
      return (com.google.protobuf.ByteString) ref;
    }
  }

  private byte memoizedIsInitialized = -1;

  @java.lang.Override
  public final boolean isInitialized() {
    byte isInitialized = memoizedIsInitialized;
    if (isInitialized == 1) return true;
    if (isInitialized == 0) return false;

    memoizedIsInitialized = 1;
    return true;
  }

  @java.lang.Override
  public void writeTo(com.google.protobuf.CodedOutputStream output) throws java.io.IOException {
    if (!getParentBytes().isEmpty()) {
      com.google.protobuf.GeneratedMessageV3.writeString(output, 1, parent_);
    }
    if (!getQueryBytes().isEmpty()) {
      com.google.protobuf.GeneratedMessageV3.writeString(output, 2, query_);
    }
    if (pageSize_ != 0) {
      output.writeInt32(3, pageSize_);
    }
    if (!getPageTokenBytes().isEmpty()) {
      com.google.protobuf.GeneratedMessageV3.writeString(output, 4, pageToken_);
    }
    unknownFields.writeTo(output);
  }

  @java.lang.Override
  public int getSerializedSize() {
    int size = memoizedSize;
    if (size != -1) return size;

    size = 0;
    if (!getParentBytes().isEmpty()) {
      size += com.google.protobuf.GeneratedMessageV3.computeStringSize(1, parent_);
    }
    if (!getQueryBytes().isEmpty()) {
      size += com.google.protobuf.GeneratedMessageV3.computeStringSize(2, query_);
    }
    if (pageSize_ != 0) {
      size += com.google.protobuf.CodedOutputStream.computeInt32Size(3, pageSize_);
    }
    if (!getPageTokenBytes().isEmpty()) {
      size += com.google.protobuf.GeneratedMessageV3.computeStringSize(4, pageToken_);
    }
    size += unknownFields.getSerializedSize();
    memoizedSize = size;
    return size;
  }

  @java.lang.Override
  public boolean equals(final java.lang.Object obj) {
    if (obj == this) {
      return true;
    }
    if (!(obj instanceof com.google.irm.service.v1alpha2.api.SearchSignalsRequest)) {
      return super.equals(obj);
    }
    com.google.irm.service.v1alpha2.api.SearchSignalsRequest other =
        (com.google.irm.service.v1alpha2.api.SearchSignalsRequest) obj;

    if (!getParent().equals(other.getParent())) return false;
    if (!getQuery().equals(other.getQuery())) return false;
    if (getPageSize() != other.getPageSize()) return false;
    if (!getPageToken().equals(other.getPageToken())) return false;
    if (!unknownFields.equals(other.unknownFields)) return false;
    return true;
  }

  @java.lang.Override
  public int hashCode() {
    if (memoizedHashCode != 0) {
      return memoizedHashCode;
    }
    int hash = 41;
    hash = (19 * hash) + getDescriptor().hashCode();
    hash = (37 * hash) + PARENT_FIELD_NUMBER;
    hash = (53 * hash) + getParent().hashCode();
    hash = (37 * hash) + QUERY_FIELD_NUMBER;
    hash = (53 * hash) + getQuery().hashCode();
    hash = (37 * hash) + PAGE_SIZE_FIELD_NUMBER;
    hash = (53 * hash) + getPageSize();
    hash = (37 * hash) + PAGE_TOKEN_FIELD_NUMBER;
    hash = (53 * hash) + getPageToken().hashCode();
    hash = (29 * hash) + unknownFields.hashCode();
    memoizedHashCode = hash;
    return hash;
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      java.nio.ByteBuffer data) throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      java.nio.ByteBuffer data, com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      byte[] data, com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return PARSER.parseFrom(data, extensionRegistry);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      java.io.InputStream input) throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageV3.parseWithIOException(PARSER, input);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      java.io.InputStream input, com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageV3.parseWithIOException(
        PARSER, input, extensionRegistry);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseDelimitedFrom(
      java.io.InputStream input) throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageV3.parseDelimitedWithIOException(PARSER, input);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseDelimitedFrom(
      java.io.InputStream input, com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageV3.parseDelimitedWithIOException(
        PARSER, input, extensionRegistry);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      com.google.protobuf.CodedInputStream input) throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageV3.parseWithIOException(PARSER, input);
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageV3.parseWithIOException(
        PARSER, input, extensionRegistry);
  }

  @java.lang.Override
  public Builder newBuilderForType() {
    return newBuilder();
  }

  public static Builder newBuilder() {
    return DEFAULT_INSTANCE.toBuilder();
  }

  public static Builder newBuilder(
      com.google.irm.service.v1alpha2.api.SearchSignalsRequest prototype) {
    return DEFAULT_INSTANCE.toBuilder().mergeFrom(prototype);
  }

  @java.lang.Override
  public Builder toBuilder() {
    return this == DEFAULT_INSTANCE ? new Builder() : new Builder().mergeFrom(this);
  }

  @java.lang.Override
  protected Builder newBuilderForType(com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
    Builder builder = new Builder(parent);
    return builder;
  }
  /**
   *
   *
   * <pre>
   * Request for the SearchSignals method.
   * </pre>
   *
   * Protobuf type {@code google.cloud.irm.v1alpha2.SearchSignalsRequest}
   */
  public static final class Builder extends com.google.protobuf.GeneratedMessageV3.Builder<Builder>
      implements
      // @@protoc_insertion_point(builder_implements:google.cloud.irm.v1alpha2.SearchSignalsRequest)
      com.google.irm.service.v1alpha2.api.SearchSignalsRequestOrBuilder {
    public static final com.google.protobuf.Descriptors.Descriptor getDescriptor() {
      return com.google.irm.service.v1alpha2.api.IncidentsService
          .internal_static_google_cloud_irm_v1alpha2_SearchSignalsRequest_descriptor;
    }

    @java.lang.Override
    protected com.google.protobuf.GeneratedMessageV3.FieldAccessorTable
        internalGetFieldAccessorTable() {
      return com.google.irm.service.v1alpha2.api.IncidentsService
          .internal_static_google_cloud_irm_v1alpha2_SearchSignalsRequest_fieldAccessorTable
          .ensureFieldAccessorsInitialized(
              com.google.irm.service.v1alpha2.api.SearchSignalsRequest.class,
              com.google.irm.service.v1alpha2.api.SearchSignalsRequest.Builder.class);
    }

    // Construct using com.google.irm.service.v1alpha2.api.SearchSignalsRequest.newBuilder()
    private Builder() {
      maybeForceBuilderInitialization();
    }

    private Builder(com.google.protobuf.GeneratedMessageV3.BuilderParent parent) {
      super(parent);
      maybeForceBuilderInitialization();
    }

    private void maybeForceBuilderInitialization() {
      if (com.google.protobuf.GeneratedMessageV3.alwaysUseFieldBuilders) {}
    }

    @java.lang.Override
    public Builder clear() {
      super.clear();
      parent_ = "";

      query_ = "";

      pageSize_ = 0;

      pageToken_ = "";

      return this;
    }

    @java.lang.Override
    public com.google.protobuf.Descriptors.Descriptor getDescriptorForType() {
      return com.google.irm.service.v1alpha2.api.IncidentsService
          .internal_static_google_cloud_irm_v1alpha2_SearchSignalsRequest_descriptor;
    }

    @java.lang.Override
    public com.google.irm.service.v1alpha2.api.SearchSignalsRequest getDefaultInstanceForType() {
      return com.google.irm.service.v1alpha2.api.SearchSignalsRequest.getDefaultInstance();
    }

    @java.lang.Override
    public com.google.irm.service.v1alpha2.api.SearchSignalsRequest build() {
      com.google.irm.service.v1alpha2.api.SearchSignalsRequest result = buildPartial();
      if (!result.isInitialized()) {
        throw newUninitializedMessageException(result);
      }
      return result;
    }

    @java.lang.Override
    public com.google.irm.service.v1alpha2.api.SearchSignalsRequest buildPartial() {
      com.google.irm.service.v1alpha2.api.SearchSignalsRequest result =
          new com.google.irm.service.v1alpha2.api.SearchSignalsRequest(this);
      result.parent_ = parent_;
      result.query_ = query_;
      result.pageSize_ = pageSize_;
      result.pageToken_ = pageToken_;
      onBuilt();
      return result;
    }

    @java.lang.Override
    public Builder clone() {
      return super.clone();
    }

    @java.lang.Override
    public Builder setField(
        com.google.protobuf.Descriptors.FieldDescriptor field, java.lang.Object value) {
      return super.setField(field, value);
    }

    @java.lang.Override
    public Builder clearField(com.google.protobuf.Descriptors.FieldDescriptor field) {
      return super.clearField(field);
    }

    @java.lang.Override
    public Builder clearOneof(com.google.protobuf.Descriptors.OneofDescriptor oneof) {
      return super.clearOneof(oneof);
    }

    @java.lang.Override
    public Builder setRepeatedField(
        com.google.protobuf.Descriptors.FieldDescriptor field, int index, java.lang.Object value) {
      return super.setRepeatedField(field, index, value);
    }

    @java.lang.Override
    public Builder addRepeatedField(
        com.google.protobuf.Descriptors.FieldDescriptor field, java.lang.Object value) {
      return super.addRepeatedField(field, value);
    }

    @java.lang.Override
    public Builder mergeFrom(com.google.protobuf.Message other) {
      if (other instanceof com.google.irm.service.v1alpha2.api.SearchSignalsRequest) {
        return mergeFrom((com.google.irm.service.v1alpha2.api.SearchSignalsRequest) other);
      } else {
        super.mergeFrom(other);
        return this;
      }
    }

    public Builder mergeFrom(com.google.irm.service.v1alpha2.api.SearchSignalsRequest other) {
      if (other == com.google.irm.service.v1alpha2.api.SearchSignalsRequest.getDefaultInstance())
        return this;
      if (!other.getParent().isEmpty()) {
        parent_ = other.parent_;
        onChanged();
      }
      if (!other.getQuery().isEmpty()) {
        query_ = other.query_;
        onChanged();
      }
      if (other.getPageSize() != 0) {
        setPageSize(other.getPageSize());
      }
      if (!other.getPageToken().isEmpty()) {
        pageToken_ = other.pageToken_;
        onChanged();
      }
      this.mergeUnknownFields(other.unknownFields);
      onChanged();
      return this;
    }

    @java.lang.Override
    public final boolean isInitialized() {
      return true;
    }

    @java.lang.Override
    public Builder mergeFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      com.google.irm.service.v1alpha2.api.SearchSignalsRequest parsedMessage = null;
      try {
        parsedMessage = PARSER.parsePartialFrom(input, extensionRegistry);
      } catch (com.google.protobuf.InvalidProtocolBufferException e) {
        parsedMessage =
            (com.google.irm.service.v1alpha2.api.SearchSignalsRequest) e.getUnfinishedMessage();
        throw e.unwrapIOException();
      } finally {
        if (parsedMessage != null) {
          mergeFrom(parsedMessage);
        }
      }
      return this;
    }

    private java.lang.Object parent_ = "";
    /**
     *
     *
     * <pre>
     * The resource name of the hosting Stackdriver project which requested
     * incidents belong to.
     * </pre>
     *
     * <code>string parent = 1;</code>
     *
     * @return The parent.
     */
    public java.lang.String getParent() {
      java.lang.Object ref = parent_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        parent_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     *
     *
     * <pre>
     * The resource name of the hosting Stackdriver project which requested
     * incidents belong to.
     * </pre>
     *
     * <code>string parent = 1;</code>
     *
     * @return The bytes for parent.
     */
    public com.google.protobuf.ByteString getParentBytes() {
      java.lang.Object ref = parent_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b =
            com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
        parent_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     *
     *
     * <pre>
     * The resource name of the hosting Stackdriver project which requested
     * incidents belong to.
     * </pre>
     *
     * <code>string parent = 1;</code>
     *
     * @param value The parent to set.
     * @return This builder for chaining.
     */
    public Builder setParent(java.lang.String value) {
      if (value == null) {
        throw new NullPointerException();
      }

      parent_ = value;
      onChanged();
      return this;
    }
    /**
     *
     *
     * <pre>
     * The resource name of the hosting Stackdriver project which requested
     * incidents belong to.
     * </pre>
     *
     * <code>string parent = 1;</code>
     *
     * @return This builder for chaining.
     */
    public Builder clearParent() {

      parent_ = getDefaultInstance().getParent();
      onChanged();
      return this;
    }
    /**
     *
     *
     * <pre>
     * The resource name of the hosting Stackdriver project which requested
     * incidents belong to.
     * </pre>
     *
     * <code>string parent = 1;</code>
     *
     * @param value The bytes for parent to set.
     * @return This builder for chaining.
     */
    public Builder setParentBytes(com.google.protobuf.ByteString value) {
      if (value == null) {
        throw new NullPointerException();
      }
      checkByteStringIsUtf8(value);

      parent_ = value;
      onChanged();
      return this;
    }

    private java.lang.Object query_ = "";
    /**
     *
     *
     * <pre>
     * An expression that defines which signals to return.
     * Search atoms can be used to match certain specific fields.  Otherwise,
     * plain text will match text fields in the signal.
     * Search atoms:
     * * `start` - (timestamp) The time the signal was created.
     * * `title` - The title of the signal.
     * * `signal_state` - `open` or `closed`. State of the signal.
     *   (e.g., `signal_state:open`)
     * Timestamp formats:
     * * yyyy-MM-dd - an absolute date, treated as a calendar-day-wide window.
     *   In other words, the "&lt;" operator will match dates before that date, the
     *   "&gt;" operator will match dates after that date, and the ":" operator will
     *   match the entire day.
     * * yyyy-MM-ddTHH:mm - Same as above, but with minute resolution.
     * * yyyy-MM-ddTHH:mm:ss - Same as above, but with second resolution.
     * * Nd (e.g. 7d) - a relative number of days ago, treated as a moment in time
     *   (as opposed to a day-wide span) a multiple of 24 hours ago (as opposed to
     *   calendar days).  In the case of daylight savings time, it will apply the
     *   current timezone to both ends of the range.  Note that exact matching
     *   (e.g. `start:7d`) is unlikely to be useful because that would only match
     *   signals created precisely at a particular instant in time.
     * The absolute timestamp formats (everything starting with a year) can
     * optionally be followed with a UTC offset in +/-hh:mm format.  Also, the 'T'
     * separating dates and times can optionally be replaced with a space. Note
     * that any timestamp containing a space or colon will need to be quoted.
     * Examples:
     * * `foo` - matches signals containing the word "foo"
     * * `"foo bar"` - matches signals containing the phrase "foo bar"
     * * `foo bar` or `foo AND bar` - matches signals containing the words
     *   "foo" and "bar"
     * * `foo -bar` or `foo AND NOT bar` - matches signals containing the
     *   word
     *   "foo" but not the word "bar"
     * * `foo OR bar` - matches signals containing the word "foo" or the
     *   word "bar"
     * * `start&gt;2018-11-28` - matches signals which started after November
     *   11, 2018.
     * * `start&lt;=2018-11-28` - matches signals which started on or before
     *   November 11, 2018.
     * * `start:2018-11-28` - matches signals which started on November 11,
     *   2018.
     * * `start&gt;"2018-11-28 01:02:03+04:00"` - matches signals which started
     *   after November 11, 2018 at 1:02:03 AM according to the UTC+04 time
     *   zone.
     * * `start&gt;7d` - matches signals which started after the point in time
     *   7*24 hours ago
     * * `start&gt;180d` - similar to 7d, but likely to cross the daylight savings
     *   time boundary, so the end time will be 1 hour different from "now."
     * * `foo AND start&gt;90d AND stage&lt;resolved` - unresolved signals from
     *   the past 90 days containing the word "foo"
     * </pre>
     *
     * <code>string query = 2;</code>
     *
     * @return The query.
     */
    public java.lang.String getQuery() {
      java.lang.Object ref = query_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        query_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     *
     *
     * <pre>
     * An expression that defines which signals to return.
     * Search atoms can be used to match certain specific fields.  Otherwise,
     * plain text will match text fields in the signal.
     * Search atoms:
     * * `start` - (timestamp) The time the signal was created.
     * * `title` - The title of the signal.
     * * `signal_state` - `open` or `closed`. State of the signal.
     *   (e.g., `signal_state:open`)
     * Timestamp formats:
     * * yyyy-MM-dd - an absolute date, treated as a calendar-day-wide window.
     *   In other words, the "&lt;" operator will match dates before that date, the
     *   "&gt;" operator will match dates after that date, and the ":" operator will
     *   match the entire day.
     * * yyyy-MM-ddTHH:mm - Same as above, but with minute resolution.
     * * yyyy-MM-ddTHH:mm:ss - Same as above, but with second resolution.
     * * Nd (e.g. 7d) - a relative number of days ago, treated as a moment in time
     *   (as opposed to a day-wide span) a multiple of 24 hours ago (as opposed to
     *   calendar days).  In the case of daylight savings time, it will apply the
     *   current timezone to both ends of the range.  Note that exact matching
     *   (e.g. `start:7d`) is unlikely to be useful because that would only match
     *   signals created precisely at a particular instant in time.
     * The absolute timestamp formats (everything starting with a year) can
     * optionally be followed with a UTC offset in +/-hh:mm format.  Also, the 'T'
     * separating dates and times can optionally be replaced with a space. Note
     * that any timestamp containing a space or colon will need to be quoted.
     * Examples:
     * * `foo` - matches signals containing the word "foo"
     * * `"foo bar"` - matches signals containing the phrase "foo bar"
     * * `foo bar` or `foo AND bar` - matches signals containing the words
     *   "foo" and "bar"
     * * `foo -bar` or `foo AND NOT bar` - matches signals containing the
     *   word
     *   "foo" but not the word "bar"
     * * `foo OR bar` - matches signals containing the word "foo" or the
     *   word "bar"
     * * `start&gt;2018-11-28` - matches signals which started after November
     *   11, 2018.
     * * `start&lt;=2018-11-28` - matches signals which started on or before
     *   November 11, 2018.
     * * `start:2018-11-28` - matches signals which started on November 11,
     *   2018.
     * * `start&gt;"2018-11-28 01:02:03+04:00"` - matches signals which started
     *   after November 11, 2018 at 1:02:03 AM according to the UTC+04 time
     *   zone.
     * * `start&gt;7d` - matches signals which started after the point in time
     *   7*24 hours ago
     * * `start&gt;180d` - similar to 7d, but likely to cross the daylight savings
     *   time boundary, so the end time will be 1 hour different from "now."
     * * `foo AND start&gt;90d AND stage&lt;resolved` - unresolved signals from
     *   the past 90 days containing the word "foo"
     * </pre>
     *
     * <code>string query = 2;</code>
     *
     * @return The bytes for query.
     */
    public com.google.protobuf.ByteString getQueryBytes() {
      java.lang.Object ref = query_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b =
            com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
        query_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     *
     *
     * <pre>
     * An expression that defines which signals to return.
     * Search atoms can be used to match certain specific fields.  Otherwise,
     * plain text will match text fields in the signal.
     * Search atoms:
     * * `start` - (timestamp) The time the signal was created.
     * * `title` - The title of the signal.
     * * `signal_state` - `open` or `closed`. State of the signal.
     *   (e.g., `signal_state:open`)
     * Timestamp formats:
     * * yyyy-MM-dd - an absolute date, treated as a calendar-day-wide window.
     *   In other words, the "&lt;" operator will match dates before that date, the
     *   "&gt;" operator will match dates after that date, and the ":" operator will
     *   match the entire day.
     * * yyyy-MM-ddTHH:mm - Same as above, but with minute resolution.
     * * yyyy-MM-ddTHH:mm:ss - Same as above, but with second resolution.
     * * Nd (e.g. 7d) - a relative number of days ago, treated as a moment in time
     *   (as opposed to a day-wide span) a multiple of 24 hours ago (as opposed to
     *   calendar days).  In the case of daylight savings time, it will apply the
     *   current timezone to both ends of the range.  Note that exact matching
     *   (e.g. `start:7d`) is unlikely to be useful because that would only match
     *   signals created precisely at a particular instant in time.
     * The absolute timestamp formats (everything starting with a year) can
     * optionally be followed with a UTC offset in +/-hh:mm format.  Also, the 'T'
     * separating dates and times can optionally be replaced with a space. Note
     * that any timestamp containing a space or colon will need to be quoted.
     * Examples:
     * * `foo` - matches signals containing the word "foo"
     * * `"foo bar"` - matches signals containing the phrase "foo bar"
     * * `foo bar` or `foo AND bar` - matches signals containing the words
     *   "foo" and "bar"
     * * `foo -bar` or `foo AND NOT bar` - matches signals containing the
     *   word
     *   "foo" but not the word "bar"
     * * `foo OR bar` - matches signals containing the word "foo" or the
     *   word "bar"
     * * `start&gt;2018-11-28` - matches signals which started after November
     *   11, 2018.
     * * `start&lt;=2018-11-28` - matches signals which started on or before
     *   November 11, 2018.
     * * `start:2018-11-28` - matches signals which started on November 11,
     *   2018.
     * * `start&gt;"2018-11-28 01:02:03+04:00"` - matches signals which started
     *   after November 11, 2018 at 1:02:03 AM according to the UTC+04 time
     *   zone.
     * * `start&gt;7d` - matches signals which started after the point in time
     *   7*24 hours ago
     * * `start&gt;180d` - similar to 7d, but likely to cross the daylight savings
     *   time boundary, so the end time will be 1 hour different from "now."
     * * `foo AND start&gt;90d AND stage&lt;resolved` - unresolved signals from
     *   the past 90 days containing the word "foo"
     * </pre>
     *
     * <code>string query = 2;</code>
     *
     * @param value The query to set.
     * @return This builder for chaining.
     */
    public Builder setQuery(java.lang.String value) {
      if (value == null) {
        throw new NullPointerException();
      }

      query_ = value;
      onChanged();
      return this;
    }
    /**
     *
     *
     * <pre>
     * An expression that defines which signals to return.
     * Search atoms can be used to match certain specific fields.  Otherwise,
     * plain text will match text fields in the signal.
     * Search atoms:
     * * `start` - (timestamp) The time the signal was created.
     * * `title` - The title of the signal.
     * * `signal_state` - `open` or `closed`. State of the signal.
     *   (e.g., `signal_state:open`)
     * Timestamp formats:
     * * yyyy-MM-dd - an absolute date, treated as a calendar-day-wide window.
     *   In other words, the "&lt;" operator will match dates before that date, the
     *   "&gt;" operator will match dates after that date, and the ":" operator will
     *   match the entire day.
     * * yyyy-MM-ddTHH:mm - Same as above, but with minute resolution.
     * * yyyy-MM-ddTHH:mm:ss - Same as above, but with second resolution.
     * * Nd (e.g. 7d) - a relative number of days ago, treated as a moment in time
     *   (as opposed to a day-wide span) a multiple of 24 hours ago (as opposed to
     *   calendar days).  In the case of daylight savings time, it will apply the
     *   current timezone to both ends of the range.  Note that exact matching
     *   (e.g. `start:7d`) is unlikely to be useful because that would only match
     *   signals created precisely at a particular instant in time.
     * The absolute timestamp formats (everything starting with a year) can
     * optionally be followed with a UTC offset in +/-hh:mm format.  Also, the 'T'
     * separating dates and times can optionally be replaced with a space. Note
     * that any timestamp containing a space or colon will need to be quoted.
     * Examples:
     * * `foo` - matches signals containing the word "foo"
     * * `"foo bar"` - matches signals containing the phrase "foo bar"
     * * `foo bar` or `foo AND bar` - matches signals containing the words
     *   "foo" and "bar"
     * * `foo -bar` or `foo AND NOT bar` - matches signals containing the
     *   word
     *   "foo" but not the word "bar"
     * * `foo OR bar` - matches signals containing the word "foo" or the
     *   word "bar"
     * * `start&gt;2018-11-28` - matches signals which started after November
     *   11, 2018.
     * * `start&lt;=2018-11-28` - matches signals which started on or before
     *   November 11, 2018.
     * * `start:2018-11-28` - matches signals which started on November 11,
     *   2018.
     * * `start&gt;"2018-11-28 01:02:03+04:00"` - matches signals which started
     *   after November 11, 2018 at 1:02:03 AM according to the UTC+04 time
     *   zone.
     * * `start&gt;7d` - matches signals which started after the point in time
     *   7*24 hours ago
     * * `start&gt;180d` - similar to 7d, but likely to cross the daylight savings
     *   time boundary, so the end time will be 1 hour different from "now."
     * * `foo AND start&gt;90d AND stage&lt;resolved` - unresolved signals from
     *   the past 90 days containing the word "foo"
     * </pre>
     *
     * <code>string query = 2;</code>
     *
     * @return This builder for chaining.
     */
    public Builder clearQuery() {

      query_ = getDefaultInstance().getQuery();
      onChanged();
      return this;
    }
    /**
     *
     *
     * <pre>
     * An expression that defines which signals to return.
     * Search atoms can be used to match certain specific fields.  Otherwise,
     * plain text will match text fields in the signal.
     * Search atoms:
     * * `start` - (timestamp) The time the signal was created.
     * * `title` - The title of the signal.
     * * `signal_state` - `open` or `closed`. State of the signal.
     *   (e.g., `signal_state:open`)
     * Timestamp formats:
     * * yyyy-MM-dd - an absolute date, treated as a calendar-day-wide window.
     *   In other words, the "&lt;" operator will match dates before that date, the
     *   "&gt;" operator will match dates after that date, and the ":" operator will
     *   match the entire day.
     * * yyyy-MM-ddTHH:mm - Same as above, but with minute resolution.
     * * yyyy-MM-ddTHH:mm:ss - Same as above, but with second resolution.
     * * Nd (e.g. 7d) - a relative number of days ago, treated as a moment in time
     *   (as opposed to a day-wide span) a multiple of 24 hours ago (as opposed to
     *   calendar days).  In the case of daylight savings time, it will apply the
     *   current timezone to both ends of the range.  Note that exact matching
     *   (e.g. `start:7d`) is unlikely to be useful because that would only match
     *   signals created precisely at a particular instant in time.
     * The absolute timestamp formats (everything starting with a year) can
     * optionally be followed with a UTC offset in +/-hh:mm format.  Also, the 'T'
     * separating dates and times can optionally be replaced with a space. Note
     * that any timestamp containing a space or colon will need to be quoted.
     * Examples:
     * * `foo` - matches signals containing the word "foo"
     * * `"foo bar"` - matches signals containing the phrase "foo bar"
     * * `foo bar` or `foo AND bar` - matches signals containing the words
     *   "foo" and "bar"
     * * `foo -bar` or `foo AND NOT bar` - matches signals containing the
     *   word
     *   "foo" but not the word "bar"
     * * `foo OR bar` - matches signals containing the word "foo" or the
     *   word "bar"
     * * `start&gt;2018-11-28` - matches signals which started after November
     *   11, 2018.
     * * `start&lt;=2018-11-28` - matches signals which started on or before
     *   November 11, 2018.
     * * `start:2018-11-28` - matches signals which started on November 11,
     *   2018.
     * * `start&gt;"2018-11-28 01:02:03+04:00"` - matches signals which started
     *   after November 11, 2018 at 1:02:03 AM according to the UTC+04 time
     *   zone.
     * * `start&gt;7d` - matches signals which started after the point in time
     *   7*24 hours ago
     * * `start&gt;180d` - similar to 7d, but likely to cross the daylight savings
     *   time boundary, so the end time will be 1 hour different from "now."
     * * `foo AND start&gt;90d AND stage&lt;resolved` - unresolved signals from
     *   the past 90 days containing the word "foo"
     * </pre>
     *
     * <code>string query = 2;</code>
     *
     * @param value The bytes for query to set.
     * @return This builder for chaining.
     */
    public Builder setQueryBytes(com.google.protobuf.ByteString value) {
      if (value == null) {
        throw new NullPointerException();
      }
      checkByteStringIsUtf8(value);

      query_ = value;
      onChanged();
      return this;
    }

    private int pageSize_;
    /**
     *
     *
     * <pre>
     * Maximum number of `signals` to return in the response.
     * </pre>
     *
     * <code>int32 page_size = 3;</code>
     *
     * @return The pageSize.
     */
    public int getPageSize() {
      return pageSize_;
    }
    /**
     *
     *
     * <pre>
     * Maximum number of `signals` to return in the response.
     * </pre>
     *
     * <code>int32 page_size = 3;</code>
     *
     * @param value The pageSize to set.
     * @return This builder for chaining.
     */
    public Builder setPageSize(int value) {

      pageSize_ = value;
      onChanged();
      return this;
    }
    /**
     *
     *
     * <pre>
     * Maximum number of `signals` to return in the response.
     * </pre>
     *
     * <code>int32 page_size = 3;</code>
     *
     * @return This builder for chaining.
     */
    public Builder clearPageSize() {

      pageSize_ = 0;
      onChanged();
      return this;
    }

    private java.lang.Object pageToken_ = "";
    /**
     *
     *
     * <pre>
     * Page token from an earlier query, as returned in `next_page_token`. All
     * field values except for page_size and page_token should be the same as the
     * original query (may return an error or unexpected data otherwise).
     * </pre>
     *
     * <code>string page_token = 4;</code>
     *
     * @return The pageToken.
     */
    public java.lang.String getPageToken() {
      java.lang.Object ref = pageToken_;
      if (!(ref instanceof java.lang.String)) {
        com.google.protobuf.ByteString bs = (com.google.protobuf.ByteString) ref;
        java.lang.String s = bs.toStringUtf8();
        pageToken_ = s;
        return s;
      } else {
        return (java.lang.String) ref;
      }
    }
    /**
     *
     *
     * <pre>
     * Page token from an earlier query, as returned in `next_page_token`. All
     * field values except for page_size and page_token should be the same as the
     * original query (may return an error or unexpected data otherwise).
     * </pre>
     *
     * <code>string page_token = 4;</code>
     *
     * @return The bytes for pageToken.
     */
    public com.google.protobuf.ByteString getPageTokenBytes() {
      java.lang.Object ref = pageToken_;
      if (ref instanceof String) {
        com.google.protobuf.ByteString b =
            com.google.protobuf.ByteString.copyFromUtf8((java.lang.String) ref);
        pageToken_ = b;
        return b;
      } else {
        return (com.google.protobuf.ByteString) ref;
      }
    }
    /**
     *
     *
     * <pre>
     * Page token from an earlier query, as returned in `next_page_token`. All
     * field values except for page_size and page_token should be the same as the
     * original query (may return an error or unexpected data otherwise).
     * </pre>
     *
     * <code>string page_token = 4;</code>
     *
     * @param value The pageToken to set.
     * @return This builder for chaining.
     */
    public Builder setPageToken(java.lang.String value) {
      if (value == null) {
        throw new NullPointerException();
      }

      pageToken_ = value;
      onChanged();
      return this;
    }
    /**
     *
     *
     * <pre>
     * Page token from an earlier query, as returned in `next_page_token`. All
     * field values except for page_size and page_token should be the same as the
     * original query (may return an error or unexpected data otherwise).
     * </pre>
     *
     * <code>string page_token = 4;</code>
     *
     * @return This builder for chaining.
     */
    public Builder clearPageToken() {

      pageToken_ = getDefaultInstance().getPageToken();
      onChanged();
      return this;
    }
    /**
     *
     *
     * <pre>
     * Page token from an earlier query, as returned in `next_page_token`. All
     * field values except for page_size and page_token should be the same as the
     * original query (may return an error or unexpected data otherwise).
     * </pre>
     *
     * <code>string page_token = 4;</code>
     *
     * @param value The bytes for pageToken to set.
     * @return This builder for chaining.
     */
    public Builder setPageTokenBytes(com.google.protobuf.ByteString value) {
      if (value == null) {
        throw new NullPointerException();
      }
      checkByteStringIsUtf8(value);

      pageToken_ = value;
      onChanged();
      return this;
    }

    @java.lang.Override
    public final Builder setUnknownFields(final com.google.protobuf.UnknownFieldSet unknownFields) {
      return super.setUnknownFields(unknownFields);
    }

    @java.lang.Override
    public final Builder mergeUnknownFields(
        final com.google.protobuf.UnknownFieldSet unknownFields) {
      return super.mergeUnknownFields(unknownFields);
    }

    // @@protoc_insertion_point(builder_scope:google.cloud.irm.v1alpha2.SearchSignalsRequest)
  }

  // @@protoc_insertion_point(class_scope:google.cloud.irm.v1alpha2.SearchSignalsRequest)
  private static final com.google.irm.service.v1alpha2.api.SearchSignalsRequest DEFAULT_INSTANCE;

  static {
    DEFAULT_INSTANCE = new com.google.irm.service.v1alpha2.api.SearchSignalsRequest();
  }

  public static com.google.irm.service.v1alpha2.api.SearchSignalsRequest getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static final com.google.protobuf.Parser<SearchSignalsRequest> PARSER =
      new com.google.protobuf.AbstractParser<SearchSignalsRequest>() {
        @java.lang.Override
        public SearchSignalsRequest parsePartialFrom(
            com.google.protobuf.CodedInputStream input,
            com.google.protobuf.ExtensionRegistryLite extensionRegistry)
            throws com.google.protobuf.InvalidProtocolBufferException {
          return new SearchSignalsRequest(input, extensionRegistry);
        }
      };

  public static com.google.protobuf.Parser<SearchSignalsRequest> parser() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.protobuf.Parser<SearchSignalsRequest> getParserForType() {
    return PARSER;
  }

  @java.lang.Override
  public com.google.irm.service.v1alpha2.api.SearchSignalsRequest getDefaultInstanceForType() {
    return DEFAULT_INSTANCE;
  }
}
